<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate @user, renderer: BootstrapPagination::Rails %>

<ul class="users">
    <%= render @users %>
</ul>

<%= will_paginate @user, renderer: BootstrapPagination::Rails %>
